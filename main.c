#include "main.h"

/*
* main - infinite loop function, at the core of the simple shell
* Return:
*/
int main(void)
{
	char *line = NULL;

	while (1)
	(
		printf("$ ");

		
	)
	free(line);
	return (0);
}
